#ifndef BUILD_INFO_H
#define BUILD_INFO_H

// This file should *only* be included from the top-level main, since it is
// regenerated with a different value on every build.

#define SHADOW_BUILD_STRING "${SHADOW_BUILD_STRING_CONF}"
#define SHADOW_INFO_STRING "${SHADOW_INFO_STRING_CONF}"

#endif
